@{
    ViewBag.Title = "Project Control PST";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4><i class="fa fa-plus"></i> New Project</h4>

@*<button onclick="createWorkOrder()" class="btn btn-default">New Work Order</button>*@
<div class="row">
    <div class="col-sm-4">
        <button type="button" onclick="location.href = '@Url.Action("InsertWorkOrder", "WorkOrder", new { typeRequest = 1 })'" class="box-project">
            <i class="fa fa-wrench"></i>
            <span>Web Development</span>
        </button>
    </div>


    <div class="col-sm-4">
        <button type="button" onclick="location.href = '@Url.Action("InsertWorkOrder", "WorkOrder", new { typeRequest = 3 })'" class="box-project">
            <i class="fa fa-file-image-o"></i>
            <span>Banners</span>
        </button>
    </div>

    <div class="col-sm-4">
        <button type="button" onclick="location.href = '@Url.Action("InsertWorkOrder", "WorkOrder", new { typeRequest = 2 })'" class="box-project">
            <i class="fa fa-pencil"></i>
            <span>Print</span>
        </button>
    </div>
</div> 

<h4><i class="fa fa-briefcase"></i> Projects</h4>
@Html.Partial("_GridView")
